import React from 'react';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

const Individual_hotel = () => {
    return (
        <View></View>
    );
}

export default Individual_hotel;